{
  "name": "rain-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ ClearRainLabs/rain-subgraph",
    "create-local": "graph create --node http://localhost:8020/ ClearRainLabs/rain-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ ClearRainLabs/rain-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ClearRainLabs/rain-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  }
}
